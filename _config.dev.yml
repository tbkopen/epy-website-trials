# Local development overrides — never deployed
url:              "http://localhost:4000"
comments_enabled: false   # no local Remark42 server by default — hide the widget entirely
remark42_host:    "http://localhost:8080"
remark42_site_id: "elastropy"
analytics:        false
show_drafts:      true
